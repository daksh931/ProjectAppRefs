export default function ProjectList({data}) {

  return (
    <>
      <span>
        {data.map(item) => 
        }
        <h2> {title}</h2>
        <p> {description} </p>
      </span>
    </>
  )
}